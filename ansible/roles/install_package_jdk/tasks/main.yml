---
# tasks file for common

- name: Ensure install packages
  apt: name={{item}} state=present
  with_items:
    - default-jdk
    - git
    - maven
    - docker.io
    - docker-compose
    - tomcat9