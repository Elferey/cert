---
# tasks file for tomcat

- name: Clone remote repository
  git:
        repo: https://github.com/Elferey/less12.git
        dest: /tmp/github
        clone: yes
        update: yes

- name: docker run
  raw: cd /tmp/github && docker-compose up -d
